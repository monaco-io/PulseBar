# PulseBar 1.8.1

[English](v1.8.1.md) | 简体中文

- 重复启动会打开已有面板，同一 macOS 用户只保留一个主实例；从不同目录启动更新后的 app 副本也有效。
- 支持并发启动，崩溃后可正常重启，无需手动清理锁文件。
- 后台采样及时回收临时对象，减少内存积压；命令行采样也增加了明确的回收边界。
- 菜单栏避免重复生成图像，图表悬停时跳过不使用的历史统计。
- 监控对象释放时会清理定时器和工作区通知监听。

支持 macOS 13 及以上、Apple Silicon 和 Intel，保留现有设置与本地事件历史。
